# TA 自動填報新手操作手冊（完全不會程式也能用）

這份手冊是給「第一次使用」的人。  
你只要照著貼指令執行，不需要理解程式細節。

---

## 0. 你要先準備什麼

1. 一台 Windows 電腦
2. 已安裝 Git（你現在已經有）
3. 這個專案資料夾（你現在在 `D:\GitHub\archie0732\TA auto script`）
4. Gemini API Key
5. 簽到單圖片（放在 `images/`）

---

## 1. 安裝 Bun（只做一次）

### 1-1 開啟 PowerShell
1. 按開始鍵，搜尋 `PowerShell`
2. 點開即可（一般權限通常就可以）

### 1-2 貼上安裝指令
```powershell
powershell -c "irm bun.sh/install.ps1|iex"
```

### 1-3 關掉 PowerShell，再重開一個新視窗，檢查是否成功
```powershell
bun --version
```

如果有出現版本號（例如 `1.x.x`）就成功。

### 1-4 如果顯示 `bun 不是內部或外部命令`
1. 先關掉終端再開一次（最常見）
2. 還是不行就把這個路徑加到環境變數 PATH：
   - `%USERPROFILE%\.bun\bin`

---

## 2. 第一次設定專案（只做一次）

在專案根目錄建立/修改 `.env`，至少要有：

```env
ACCOUNT=你的學校帳號
PASSWORD=你的學校密碼
LOGIN_WEBSITE=https://ta.pu.edu.tw/login.php
SCRIPT_WEBSITE=https://ta.pu.edu.tw/index.php
GEMINI_API_KEY=你的金鑰
GEMINI_MODEL=gemini-3.1-flash-lite
```

---

## 3. 每次操作的標準流程

## 步驟 A：放圖片
把圖片放到：
- `images/`

例如：
- `images/image_7.jpg`
- `images/image_8.jpg`

---

## 步驟 B：辨識圖片
```powershell
bun gemini/recognize-images.mjs --overwrite
```

完成後會產生：
- `data/recognized/*.json`（每張圖一份）
- `data/pending/pending.json`（待送審清單）

---

## 步驟 C：送審（核准）

### C-1 先看有哪些待送審
```powershell
bun gemini/approve-records.mjs --list
```

### C-2 只核准特定資料（例如只核准 image_7 / image_8）
先從 `--list` 找到它們的 `id`，再執行：
```powershell
bun gemini/approve-records.mjs --ids <image7_id>,<image8_id>
```

### C-3 或核准全部
```powershell
bun gemini/approve-records.mjs --all
```

---

## 步驟 D：檢查本次待寫入清單
```powershell
Get-Content data/approved/to-submit.json
```

重點：
- 這份 `to-submit.json` 預設只放「本次新核准」資料
- 不會自動帶上你上次已經送過的資料（避免重複）

---

## 步驟 E：先模擬，再正式寫入

### E-1 先 dry-run（不會真的寫入）
```powershell
bun crawler/submit-records.mjs --input data/approved/to-submit.json
```

### E-2 再 commit（真的寫入）
```powershell
bun crawler/submit-records.mjs --input data/approved/to-submit.json --commit
```

---

## 4. 你最常遇到的錯誤與解法

## 問題 1：`to-submit.json` 找不到
代表你還沒做送審（approve）。

請先跑：
```powershell
bun gemini/approve-records.mjs --all
```

---

## 問題 2：Gemini 回 `429 quota exceeded`
代表 API key 配額用完或未開通。

你要去 Gemini/Google AI 後台確認：
1. 專案是否開啟 Gemini API
2. 是否有可用配額/計費
3. key 是否拿錯專案

---

## 問題 3：頁面看起來沒新增
先確認：
1. 你是不是選錯月份
2. `submit-result.json` 裡狀態是否真的 `submitted`
3. 如果同日期已存在，系統會改「更新既有列」而非新增新列（這是正常）

---

## 問題 4：課輔內容不是「指導作頁」
目前腳本預設使用：
- `指導作頁`

如果你自己手動改了參數，可以強制指定：
```powershell
bun crawler/submit-records.mjs --input data/approved/to-submit.json --commit --notes 指導作頁
```

---

## 5. 建議你每次都照這個順序跑

```powershell
# 1) 辨識
bun gemini/recognize-images.mjs --overwrite

# 2) 看待送審
bun gemini/approve-records.mjs --list

# 3) 核准（可改成 --ids）
bun gemini/approve-records.mjs --all

# 4) 模擬寫入
bun crawler/submit-records.mjs --input data/approved/to-submit.json

# 5) 正式寫入
bun crawler/submit-records.mjs --input data/approved/to-submit.json --commit
```

---

## 6. 官方參考（Bun 安裝）
- Bun Installation（官方）：https://bun.com/docs/installation

